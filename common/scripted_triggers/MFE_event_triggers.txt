ally_in_war_short_on_goods_trigger = {
	any_scope_war = {
		count > 0
	}
	any_scope_ally = {
		root = {
			is_in_war_together = prev
			NOT = {
				has_war_with = prev
			}
		}
		market.mg:$goods$ = {
			market_goods_has_goods_shortage = yes
		}
	}
}